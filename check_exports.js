import * as Icons from '@icons-pack/react-simple-icons';
console.log("Netflix:", Object.keys(Icons).filter(k => k.toLowerCase().includes('netflix')));
console.log("Prime:", Object.keys(Icons).filter(k => k.toLowerCase().includes('prime')));
console.log("Amazon:", Object.keys(Icons).filter(k => k.toLowerCase().includes('amazon')));
console.log("YouTube:", Object.keys(Icons).filter(k => k.toLowerCase().includes('youtube')));
console.log("Crunchyroll:", Object.keys(Icons).filter(k => k.toLowerCase().includes('crunchyroll')));
